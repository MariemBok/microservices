debug: false
server:
  port: 9082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
spring:
  application:
    name: microservice-candidat
  h2:
  console:
    enabled: true
    path: /h2
  datasource:
    url: jdbc:h2:file:~/test
    username: ms_candidat
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create

